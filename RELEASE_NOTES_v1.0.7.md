# Inhouse Read v1.0.7

- Fixed the Android status bar disappearing on current Android versions.
- The native shell now explicitly keeps the system bars visible and applies status bar, navigation bar, and display cutout insets to the WebView so page content stays below the system controls.
- The Android APK remains a small WebView loader that opens the live app.
